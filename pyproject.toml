[project]
name = "netty"
version = "0.6.4"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "argcomplete>=3.6.3",
    "pyserial>=3.5",
]

[project.scripts]
netty = "main:main"
